# 🍪 Architecture du Système de Gestion des Cookies

## Vue d'ensemble du système

```
┌─────────────────────────────────────────────────────────────────────┐
│                         NAVIGATEUR DE L'UTILISATEUR                  │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │                               │
        ┌───────────▼──────────┐      ┌────────────▼─────────┐
        │   Première visite ?  │      │  Visite ultérieure   │
        │   (Pas de consent)   │      │  (Consent existant)  │
        └───────────┬──────────┘      └────────────┬─────────┘
                    │                               │
        ┌───────────▼──────────┐                    │
        │  Afficher bannière   │                    │
        │  de consentement     │                    │
        └───────────┬──────────┘                    │
                    │                               │
        ┌───────────▼──────────────────────────────┐│
        │         L'utilisateur choisit :          ││
        │  ┌────────────────────────────────────┐  ││
        │  │  1. Tout accepter                  │  ││
        │  │  2. Personnaliser (choisir par     │  ││
        │  │     catégorie)                     │  ││
        │  │  3. Tout refuser                   │  ││
        │  └────────────────────────────────────┘  ││
        └───────────┬──────────────────────────────┘│
                    │                               │
        ┌───────────▼───────────────────────────────▼────┐
        │  Stocker le consentement dans localStorage     │
        │  { essential: true, analytics: ?, marketing: ? }│
        └───────────┬────────────────────────────────────┘
                    │
        ┌───────────▼────────────────────────────────────┐
        │  Déclencher événement 'cookieConsentUpdated'   │
        └───────────┬────────────────────────────────────┘
                    │
        ┌───────────▼────────────────────────────────────┐
        │  Charger les services autorisés :              │
        │  • Analytics (si consent.analytics === true)   │
        │  • Marketing (si consent.marketing === true)   │
        └────────────────────────────────────────────────┘
```

## Flux de décision des cookies

```
┌─────────────────────────────────────────────────────────────┐
│                   PAGE HTML CHARGÉE                         │
└─────────────────┬───────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────┐
│  cookie-consent.js s'initialise                             │
│  • Vérifie localStorage['cookie_consent']                   │
└─────────────────┬───────────────────────────────────────────┘
                  │
      ┌───────────┴──────────┐
      │                      │
┌─────▼──────┐      ┌────────▼────────┐
│ Trouvé ?   │      │  Pas trouvé ?   │
│    OUI     │      │      NON        │
└─────┬──────┘      └────────┬────────┘
      │                      │
┌─────▼──────┐      ┌────────▼────────────────┐
│ Afficher   │      │ Afficher bannière       │
│ bouton     │      │ (consentement requis)   │
│ flottant   │      └────────┬────────────────┘
│            │               │
│ Appliquer  │      ┌────────▼────────────────┐
│ le consent │      │ Utilisateur interagit   │
└────────────┘      └────────┬────────────────┘
                             │
                    ┌────────▼────────────────┐
                    │ Sauvegarder choix       │
                    │ Appliquer préférences   │
                    │ Afficher bouton flottant│
                    └─────────────────────────┘
```

## Architecture des fichiers

```
harmonie-sens-website/
│
├── templates/
│   ├── base.html.twig                      [Modifié: Include cookie-consent]
│   │
│   ├── components/
│   │   ├── cookie-consent.html.twig        [NOUVEAU: Bannière + UI]
│   │   └── footer.html.twig                [Modifié: Lien "Gérer cookies"]
│   │
│   └── example/
│       └── cookie-demo.html.twig           [NOUVEAU: Page de test]
│
├── public/
│   ├── css/
│   │   └── global.css                      [Modifié: +350 lignes CSS]
│   │
│   └── js/
│       ├── cookie-consent.js               [NOUVEAU: Logique principale]
│       └── tracking-integration.js         [NOUVEAU: Services tiers]
│
├── src/
│   └── Controller/
│       └── CookieDemoController.php        [NOUVEAU: Route /cookie-demo]
│
└── docs/
    ├── COOKIE_CONSENT_GUIDE.md             [NOUVEAU: Guide complet]
    └── COOKIE_SYSTEM_CHANGELOG.md          [NOUVEAU: Historique]
```

## Cycle de vie d'une catégorie de cookies

```
┌──────────────────────────────────────────────────────────────────┐
│                  CATÉGORIE: ANALYTICS                            │
└──────────────────────────────────────────────────────────────────┘

1. CHARGEMENT INITIAL
   ├─ checkCookieConsent('analytics') → false
   ├─ Google Analytics NON chargé
   └─ Matomo NON chargé

2. UTILISATEUR ACCEPTE
   ├─ Consentement sauvegardé: { analytics: true }
   ├─ Événement 'cookieConsentUpdated' déclenché
   └─ Scripts écoutent l'événement

3. ACTIVATION
   ├─ enableAnalytics() appelée
   ├─ Google Analytics chargé dynamiquement
   ├─ Matomo chargé dynamiquement
   └─ Console: "📊 Cookies analytiques activés"

4. TRACKING ACTIF
   ├─ window.trackEvent('Category', 'Action', 'Label', 123)
   ├─ → ga('send', 'event', ...)
   └─ → _paq.push(['trackEvent', ...])

5. UTILISATEUR REFUSE (changement de préférence)
   ├─ Consentement mis à jour: { analytics: false }
   ├─ Événement 'cookieConsentUpdated' déclenché
   ├─ disableAnalytics() appelée
   └─ Cookies _ga, _gid, _gat supprimés
```

## Interaction utilisateur

```
┌─────────────────────────────────────────────────────────────────┐
│                      BANNIÈRE DE CONSENTEMENT                    │
│                                                                  │
│  🍪 Respect de votre vie privée                                 │
│                                                                  │
│  Nous utilisons des cookies pour améliorer votre expérience...  │
│                                                                  │
│  [▼ En savoir plus]  ← Clic pour développer                     │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ 🔐 Cookies essentiels (obligatoires)                       │ │
│  │     Ces cookies sont nécessaires...                        │ │
│  │                                                             │ │
│  │ 📊 Cookies analytiques                [Toggle ON/OFF]      │ │
│  │     Ces cookies nous permettent...                         │ │
│  │                                                             │ │
│  │ 🎯 Cookies marketing                  [Toggle ON/OFF]      │ │
│  │     Ces cookies permettent...                              │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  [✓ Tout accepter] [⚙ Personnaliser] [✗ Tout refuser]          │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

                              ↓

┌─────────────────────────────────────────────────────────────────┐
│  APRÈS CHOIX: Bannière disparaît, bouton flottant apparaît     │
│                                                                  │
│  [🍪]  ← Bouton flottant en bas à gauche                        │
│        (Clic pour rouvrir la bannière)                          │
└─────────────────────────────────────────────────────────────────┘
```

## API JavaScript disponible

```javascript
// ──────────────────────────────────────────────────────────────
// VÉRIFIER LE CONSENTEMENT
// ──────────────────────────────────────────────────────────────
window.checkCookieConsent('analytics')    // → true/false
window.checkCookieConsent('marketing')    // → true/false

// ──────────────────────────────────────────────────────────────
// OBTENIR LE CONSENTEMENT COMPLET
// ──────────────────────────────────────────────────────────────
window.cookieConsent.getConsent()
// → { essential: true, analytics: true, marketing: false, timestamp: ... }

// ──────────────────────────────────────────────────────────────
// AFFICHER/MASQUER LA BANNIÈRE
// ──────────────────────────────────────────────────────────────
window.cookieConsent.showBanner()
window.cookieConsent.hideBanner()

// ──────────────────────────────────────────────────────────────
// ÉCOUTER LES CHANGEMENTS
// ──────────────────────────────────────────────────────────────
document.addEventListener('cookieConsentUpdated', (event) => {
    console.log('Nouveau consentement:', event.detail);
    
    if (event.detail.analytics) {
        // Charger analytics
    }
});

// ──────────────────────────────────────────────────────────────
// TRACKER DES ÉVÉNEMENTS (nécessite tracking-integration.js)
// ──────────────────────────────────────────────────────────────
window.trackEvent('Category', 'Action', 'Label', 123)
window.trackConversion('Purchase', 99.99)
```

## Intégration avec services tiers

```
┌──────────────────────────────────────────────────────────────────┐
│              FICHIER: tracking-integration.js                     │
└──────────────────────────────────────────────────────────────────┘

const TRACKING_CONFIG = {
    googleAnalytics: {
        enabled: false,          ← Passer à true pour activer
        trackingId: 'UA-XXXXX',  ← Votre ID
        requireConsent: true,     ← Demande le consentement
        category: 'analytics'     ← Catégorie utilisée
    }
}

               ↓ Si enabled = true ET consent = true
               
┌──────────────────────────────────────────────────────────────────┐
│  loadGoogleAnalytics() est appelée                               │
│  • Injection du script analytics.js                              │
│  • Initialisation avec votre ID                                  │
│  • ga('create', 'UA-XXXXX', 'auto')                              │
│  • ga('send', 'pageview')                                        │
└──────────────────────────────────────────────────────────────────┘
```

## Stockage localStorage

```javascript
// Structure du consentement stocké
localStorage['cookie_consent'] = {
    "essential": true,      // Toujours true (obligatoire)
    "analytics": true,      // Choix de l'utilisateur
    "marketing": false,     // Choix de l'utilisateur
    "timestamp": 1708358400000  // Date du consentement
}
```

## Flux RGPD

```
┌─────────────────────────────────────────────────────────────┐
│                      CONFORMITÉ RGPD                        │
└─────────────────────────────────────────────────────────────┘

1. TRANSPARENCE
   ✅ Information claire sur les cookies utilisés
   ✅ Catégorisation des cookies
   ✅ Description de chaque catégorie

2. CONSENTEMENT
   ✅ Demandé AVANT le dépôt des cookies
   ✅ Libre et spécifique
   ✅ Facilement révocable

3. DROITS DE L'UTILISATEUR
   ✅ Accepter tout
   ✅ Refuser tout
   ✅ Choisir par catégorie
   ✅ Modifier à tout moment (bouton flottant + lien footer)

4. TECHNIQUE
   ✅ Pas de cookies tiers avant consentement
   ✅ Suppression des cookies si refusés
   ✅ Pas de cookie wall (accès au site même si refus)

5. DURÉE
   ℹ️  Consentement conservé indéfiniment (localStorage)
   ℹ️  CNIL recommande 13 mois max (à implémenter si besoin)
```

## Points d'extension

```
┌──────────────────────────────────────────────────────────────────┐
│              COMMENT AJOUTER UNE NOUVELLE CATÉGORIE              │
└──────────────────────────────────────────────────────────────────┘

1. TEMPLATE (cookie-consent.html.twig)
   Ajouter une nouvelle section dans #cookie-details

2. JAVASCRIPT (cookie-consent.js)
   • Ajouter 'nouvelleCatégorie' dans acceptAll()
   • Ajouter 'nouvelleCatégorie' dans acceptSelected()
   • Ajouter 'nouvelleCatégorie' dans refuseAll()
   • Créer enableNouvelleCatégorie()
   • Créer disableNouvelleCatégorie()

3. CSS (global.css)
   Styles déjà génériques, normalement pas de modification

4. TRACKING (tracking-integration.js)
   Ajouter configuration pour nouveaux services
```

---

**Légende des symboles :**
- ✅ = Implémenté et fonctionnel
- ℹ️  = Information importante
- 🍪 = Relatif aux cookies
- 📊 = Relatif aux analytics
- 🎯 = Relatif au marketing
- 🔐 = Relatif à la sécurité
